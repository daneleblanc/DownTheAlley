using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LaunchBowlingBall : MonoBehaviour
{

    private GameObject ball;

    // Start is called before the first frame update
    void Start()
    {
        //var create : GameObject = create.rigidbody.AddForce(Vector3(0, 1, 0));
    }

    // Update is called once per frame
    void Update()
    {
        GameObject ball;
    }
}
